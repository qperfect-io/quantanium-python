name: Pypi
on:
  workflow_run:
    workflows: ["Wheels"]
    types: 
      - completed

jobs:
  pypi_upload:
    if: github.repository == 'qperfect.io/quantanium-python-private' and ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The Wheels workflow passed'

  my_test:
    runs-on: ubuntu-latest
    steps:
    - run: echo 'Testing no conditions'
